# 0. Sequential

Builds a neural network using the Keras Sequential API with Dense layers, He Normal initialization, L2 regularization, and Dropout.

**Function:**

```python
build_model(nx, layers, activations, lambtha, keep_prob)
```
